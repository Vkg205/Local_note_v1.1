<Window x:Class="LocalNote.App.Dialogs.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入" Height="170" Width="420" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" FontSize="14" Margin="3,0,3,6"/>
        <TextBox x:Name="ValueTextBox" Grid.Row="1" FontSize="14" MinWidth="350" KeyDown="ValueTextBox_KeyDown"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="取消" IsCancel="True" Width="80"/>
            <Button Content="确定" IsDefault="True" Width="80" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
